import 'package:flutter/material.dart';

class ValidacionCodigoNumero extends StatelessWidget {
  const ValidacionCodigoNumero({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ola'),
        leading: const BackButton(),
        backgroundColor: Colors.white,
        elevation: 1,
        foregroundColor: Colors.black,
      ),
      body: Center(
        child: Text('Actualizxadaaaa'),
      ),
    );
  }
}
