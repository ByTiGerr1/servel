import 'package:flutter/material.dart';

class FavoritesCandidatesScreen extends StatelessWidget {
  const FavoritesCandidatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Candidatos Favoritos"),
      ),
    );
  }
}